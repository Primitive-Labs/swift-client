TEST_HTTP_URL=http://localhost:8787
TEST_WS_URL=ws://localhost:8787
# Super-admin JWT for the local dev server.
# Mint one with: node -e "const jwt = require('jsonwebtoken'); console.log(jwt.sign({adminId:'YOUR_ADMIN_ID',email:'you@example.com',name:'Your Name',role:'super-admin',isSuperAdmin:true,appCreationLimit:50,type:'admin',enableTestFeatures:true},'test-jwt-secret-only-for-tests',{expiresIn:'24h'}))"
TEST_SUPERADMIN_JWT=
