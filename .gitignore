.build/
build/
.swiftpm/
Package.resolved
*.xcodeproj/
xcuserdata/
DerivedData/

# Test config — holds a super-admin JWT; use .env.tests.example as the template.
# The nightly release (pl-nightly-release) mints this for the Swift test step.
.env.tests
